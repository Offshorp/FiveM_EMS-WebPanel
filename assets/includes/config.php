<?php
  // Site Name
  $site_name = "EMS WebPanel";

  // Site Description
  $site_desc = "EMS WebPanel is a basic CMS for GTA V RP";

  // Server Name (Empty to remove)
  $server_name = "CFX Server";

  // Site Lang
  $site_lang = "fr";

  // Access to Register Page
  // If "true", anyone can create an account
  // If "false", only admins can create an account
  $registration = true;

  // Admin Account Validation
  // WARNING: If $registration is "true", let $admin_validation on "true" too
  // If "true", anyone can create an account but need an admin validation
  // If "false", anyone can create an account without admin validation
  $admin_validation = true;

  // Logo on Login Page
  // If "true", it's the "Los Santos Medical Service" logo (lsmc.png)
  // If "false", it's the Site Name
  $use_loglogo = true;

  // Database Connection Infos
  $db__info = [
    'localhost', // Host
    'root', // Username
    '', // Password
    'fivem_ems' // Database
  ];
?>
